export function sendStreamingData(data: any) {
  // Essa função seria responsável por enviar atualizações de dados do telão para os clientes
  // usando tecnologias como WebSockets ou Server-Sent Events (SSE)
  console.log("Enviando dados de streaming para o telão:", data);
  // Aqui você pode adicionar a lógica de SSE ou WebSocket para comunicação em tempo real
}
